# Auth & secrets
auths/
data/
*.key
*.pem

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
/login
/credit
/wb2api
/signin_bin

# Config with secrets (use config.example.json)
config.json

# Build artifacts
*.tmp
__pycache__/

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md
docs/

# 设计/计划文档（但 README.md 必须进版本库）
*.md
!README.md

# defaultprompt.md 是**运行时资源**，不是文档：它由 //go:embed 编进二进制，
# 是「自定义系统提示词」功能的内置默认值。被上面的 *.md 误伤后本地能构建
#（文件在磁盘上），CI 却会 `pattern defaultprompt.md: no matching files found`
# 直接失败 —— 这类「本地过、CI 挂」最难查，故显式放行。
!internal/prompt/defaultprompt.md

# CAPTURED-SPEC.md 是**一手规格**，不是「设计/计划文档」—— 它记录的是
# 抓包实测的上游契约（端点、请求头、协议、权益结构），性质与 README 相同：
# 是后续维护者**必须对照**的依据，而不是某个阶段的临时计划。
#
# 为什么值得破例放行：我们曾照着**二手注释**（某参考实现的 identity.py）
# 实现 ZCode 通道，而那条注释被实测推翻（它说 x-query-id/x-session-id
# 不能发，抓包显示官方实际在发）。把一手规格与代码放在一起，
# 后人就不必再去翻抓包记录 —— 也不会再被二手结论带偏。
!internal/zcode/CAPTURED-SPEC.md

