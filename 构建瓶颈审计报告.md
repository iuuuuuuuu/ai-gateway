# workbuddy2api 构建依赖与流程瓶颈审计报告

**日期**：2026-09-15
**范围**：Rust workspace（4 crate）+ Tauri 2 桌面端 + Vite/React 前端 + Go 网关 + GitHub Actions
**方法**：12 个维度并行深挖 → 逐条反驳式独立验证 → 完整性批评补漏。85 个 agent，两轮共 **56 条经证实发现**、**10 条被证伪**、**20 条经修正**。
**关键原则**：本报告所有数字均为**本机实测**，凡未能独立证实的推断已明确标注。

---

## 一、结论速览

"AI 流程会很缓慢"不是一个原因，而是**四层叠加**的：

| 层级 | 问题 | 后果 | 修复成本 |
|------|------|------|---------|
| **能力层** | `cargo` 不在 PATH | 所有 Rust/Tauri 命令直接失败，AI 只能反复换写法重试 | ✅ **已修复** |
| **工具层** | `pwsh` 装了但不在 PATH；`build-signed.ps1` 缺 BOM | AGENTS.md 钦定的入口在 5.1 下跑不起来 | ✅ **pwsh 已修复**；BOM 待改 |
| **编译层** | `build.rs` 塞 15.5MB 字面量 + 永久脏的监听 | 每次 `cargo check` 固定白花 ~6.5 秒 | 改 1 个文件 |
| **CI 层** | 两个 workflow 触发条件完全相同 | 每次打 tag 跑两条完整流水线 | 改 yml |

**已完成（2026-09-15）**：`C:\Users\T1367\.cargo\bin` 与 `C:\Users\T1367\AppData\Local\Microsoft\WindowsApps` 已加入用户级 PATH，并清理了重复条目。验证结果：

```
cargo -V   → cargo 1.95.0 (f2d3ce0bd 2026-03-21)
pwsh       → 7.6.5
tauri info → ✔ rustc / ✔ cargo / ✔ rustup / ✔ Rust toolchain   （此前全是 ✘）
```

**最容易见效的两件事**（按性价比排序）：

1. **修 `build.rs`**（10 分钟，每次构建省 ~6.5 秒，且是下游全部 crate 的级联成本）
2. **修 `build-signed.ps1` 的 BOM**（10 秒，让签名构建入口在 `powershell.exe` 下也能用）

---

## 二、能力层：cargo 不在 PATH（最高优先级）

### 实测证据

```
$ cargo -V
bash: line 1: cargo: command not found          # 退出码 127

$ powershell.exe -NoProfile -Command "Get-Command cargo,rustc -EA SilentlyContinue"
                                                  # 无输出，退出码 1
$ [Environment]::GetEnvironmentVariable('Path','User')    # 用户级 PATH
   → 无 .cargo\bin
$ [Environment]::GetEnvironmentVariable('Path','Machine') # 系统级 PATH
   → 无 .cargo\bin
```

但 cargo **确实装了**：

```
$ ~/.cargo/bin/cargo.exe -V
cargo 1.95.0 (f2d3ce0bd 2026-03-21)
rustc 1.95.0 (59807616e 2026-04-14)   host: x86_64-pc-windows-msvc
```

### 为什么这会让 AI 很慢

验证 agent 做了**端到端对照实验**，这是最有力的证据：

```
# 无 PATH（现状）
$ node node_modules/@tauri-apps/cli/tauri.js build --no-bundle
failed to run 'cargo metadata' command to get workspace directory:
  failed to run command `cargo metadata --no-deps --format-version 1`: program not found

# 仅加 PATH（对照组）
$ export PATH="/c/Users/T1367/.cargo/bin:$PATH"
$ node node_modules/@tauri-apps/cli/tauri.js build --no-bundle
Info Looking up installed tauri packages...
Running beforeBuildCommand `npm run build`    ← 正常走通
Finished `release` profile [optimized] target(s) in 1m 44s
```

**关键：`npm run tauri` 无法绕过这个问题。** `node_modules/.bin/tauri.cmd` 全文只是 `node tauri.js %*` 的转发壳，二进制里也没有任何 `%USERPROFILE%\.cargo\bin` 回退逻辑（字符串搜索 0 命中）。所以 AI 在 `cargo: command not found` 之后改试 `npm run tauri -- build`，会**撞上同一个错误**，然后继续换下一种写法——这正是"反复试探、整体很慢"的具体形态。

更糟的是 `tauri info` 会输出：

```
✘ rustc: not installed!
✘ Cargo: not installed!
Maybe you don't have rust installed! Visit https://rustup.rs/
```

这会诱导走向"重装 rustup"这条完全错误、代价极高的路线。

### 修复

```powershell
# 读取用户级 PATH（不要用 $env:Path，那会把系统项复制进用户级）
$userPath = [Environment]::GetEnvironmentVariable('Path','User')
if ($userPath -notlike '*\.cargo\bin*') {
    [Environment]::SetEnvironmentVariable('Path', ($userPath.TrimEnd(';') + ';%USERPROFILE%\.cargo\bin'), 'User')
    Write-Host '已追加 %USERPROFILE%\.cargo\bin 到用户 PATH'
}
```

改完**必须重启终端和 IDE**（已运行的进程不继承新 PATH）。当前会话临时生效：

```bash
export PATH="/c/Users/T1367/.cargo/bin:$PATH"
```

> ⚠️ Git Bash 下**不要**写 `$USERPROFILE/.cargo/bin`——那是反斜杠形式，`which` 静默搜不到。必须用 POSIX 正斜杠。

**验证**（三项应全部通过）：

```powershell
Get-Command cargo,rustc,rustup | Select-Object Name,Source
cargo -V
node node_modules/@tauri-apps/cli/tauri.js info    # rustc/Cargo/rustup 应变成 ✔
```

---

## 三、编译层：build.rs 是最大的单一耗时源

### 3.1 把 15.5MB 字节数组塞进源文件

`crates/ai-gateway-core/build.rs:72-80`：

```rust
std::fs::write(&gen_file, format!(... "\nSome(&{:?})", raw.len(), gz.len(), gz))
```

`{:?}` 对 `Vec<u8>` 生成 `[31, 139, 8, 0, ...]` 的**十进制字面量**。实测产物：

| 项目 | 实测值 |
|------|--------|
| 原始 gateway.exe | 8,404,480 字节 |
| gzip 压缩后 | 3,381,728 字节 |
| 生成的 `gateway_embed.rs` | **15,534,259 字节**（文本放大 4.59 倍） |
| target 下堆积的副本 | **21 份，合计 325,364,982 字节（310 MB）** |
| 去重后实际只有 | **6 种内容** |

消费侧 `crates/ai-gateway-core/src/modules/gateway_embed.rs:22`：

```rust
static EMBEDDED_GZ: Option<&'static [u8]> = include!(concat!(env!("OUT_DIR"), "/gateway_embed.rs"));
```

### 3.2 实测对比

我本机独立复现（rustc 1.95.0，`--emit=metadata --crate-type rlib`）：

| 方案 | 编译耗时 |
|------|---------|
| `Some(&[u8; N])` 十进制字面量（当前） | **8,791 ms** |
| `include_bytes!` 引用 3.4MB gz 文件 | **511 ms** |

**快 17 倍**。审计 agent 用仓库真实文件复现得更彻底：净开销 ≈10.0s vs ≈8ms，**倍率 >1000 倍**（含 rustc 启动开销口径 ~60 倍）。

而 gzip 本身只花 0.33 秒——**瓶颈完全在 rustc 解析那 338 万个整数 token**。

### 3.3 更致命的是：它每次必重跑

`build.rs:36-38` 无条件注册所有候选路径：

```rust
for c in candidate_paths() {
    println!("cargo:rerun-if-changed={}", c.display());
}
```

`candidate_paths()` 里有仓库根 `dist/gateway.exe`，而**该路径不存在**。Cargo 对"rerun-if-changed 指向不存在的路径"的语义是**永久视为脏**。

实测复现（我本机跑的，什么都没改）：

```
第 1 次: 6743 ms   Compiling ai-gateway-core v0.8.0 ... Finished in 6.19s
第 2 次: 6906 ms   Compiling ai-gateway-core v0.8.0 ... Finished in 6.38s
```

**100% 复现**——这个 crate 永远无法命中缓存，而它是 `ai-gateway-router`、`ai-gateway-server`、`src-tauri` 的共同依赖，改动会级联。

### 3.4 修复方案

**`crates/ai-gateway-core/build.rs`** 核心改动：

```rust
// 1) 压缩产物写成独立的二进制文件，而不是塞进 .rs 源码
let gz_file = out_dir.join("gateway.gz");
std::fs::write(&gz_file, &gz).expect("写入 gateway.gz 失败");

// 2) 生成的 .rs 只包含一个 include_bytes!，从 15MB 降到几百字节
std::fs::write(
    &gen_file,
    "// 由 build.rs 生成：内嵌网关\nSome(include_bytes!(concat!(env!(\"OUT_DIR\"), \"/gateway.gz\")))",
).expect("写入 gateway_embed.rs 失败");

// 3) 压缩档位从 best() 降到 6：时间 385ms→133ms，体积只大 2.5%
let mut enc = flate2::write::GzEncoder::new(Vec::new(), flate2::Compression::new(6));

// 4) 只在【存在】的路径上注册监听；不存在的注册其父目录
fn watch(c: &Path) {
    if c.exists() {
        println!("cargo:rerun-if-changed={}", c.display());
    } else if let Some(p) = c.parent() {
        if p.exists() {
            println!("cargo:rerun-if-changed={}", p.display());
        }
    }
}
```

> ⚠️ **关键约束**：`include_bytes!` 指向缺失文件是**硬编译错误**（实测 `error: couldn't read ...: os error 2`）。所以找不到网关时 build.rs **必须写出 0 字节的 `gateway.gz`**，消费侧改用 `EMBEDDED_GZ.is_empty()` 判定——这与旧 `None` 语义完全等价（已验证可编译且行为一致）。

**消费侧** `gateway_embed.rs:22` 需同步改 4 行：

```rust
// 旧
static EMBEDDED_GZ: Option<&'static [u8]> = include!(concat!(env!("OUT_DIR"), "/gateway_embed.rs"));
// 新
static EMBEDDED_GZ: &'static [u8] = include_bytes!(concat!(env!("OUT_DIR"), "/gateway.gz")).as_slice();
```

`has_embedded()` / `materialize()` / `cleanup_old()` 三个公开函数签名不变，`gateway.rs` 的 6 处调用点无需改动。**不影响** updater 签名、NSIS 打包、macOS/Linux 构建（build.rs 的平台分支逻辑保留）。

### 3.5 另一半来源：tauri-build 监听 dist/

`tauri-build-2.6.3/src/codegen/context.rs:87-91` 对 `frontendDist`（`../dist`）**只判 exists()，不比较 mtime**：

```rust
Some(FrontendDist::Directory(p)) => {
  let dist_path = config_parent.join(p);
  if dist_path.exists() {
    println!("cargo:rerun-if-changed={}", dist_path.display());
  }
}
```

`dist/` 被 .gitignore 排除但 `npm run build` 频繁重写 → **前端一构建就击穿整条 Rust 链**。

**建议**：不为这个改第三方 crate。开发时用 `cargo check -p ai-gateway-core`（而非全 workspace）避开 src-tauri 与 dist/ 的耦合。

---

## 四、工具层：AGENTS.md 的入口命令在本机跑不起来

### 4.1 pwsh 装了，但不在 PATH（⚠️ 本节已更正）

> **更正说明（2026-09-15 追加）**：本报告初稿断言"本机未安装 pwsh"，**这是错误的**。经复核，pwsh **已安装**（MSIX/Store 版 PowerShell 7.6.5）。误判原因见下。

实测 pwsh 的真实位置：

```
Get-Command pwsh                        → 找不到
Test-Path 'C:\Program Files\PowerShell\7\pwsh.exe'   → False   ← 这是 MSI 版的位置
Test-Path "$env:LOCALAPPDATA\Microsoft\WindowsApps\pwsh.exe"  → True  ← 0 字节
Get-AppxPackage Microsoft.PowerShell
  ├ Name            : Microsoft.PowerShell
  ├ Version         : 7.6.5.0
  ├ InstallLocation : C:\Program Files\WindowsApps\Microsoft.PowerShell_7.6.5.0_x64__8wekyb3d8bbwe
  └ Status          : Ok
```

**误判的两个原因**：

1. **MSIX 版 pwsh 的 exe 是"App Execution Alias"桩文件**：`WindowsApps\pwsh.exe` 长度是 **0 字节**（重解析点），真正的程序在 `C:\Program Files\WindowsApps\...` 下。我探测时只查了 **MSI 版**的安装位置 `C:\Program Files\PowerShell\7\`，所以全判 False。
2. **`WindowsApps` 目录当时不在 PATH 中**：`Get-Command` 查的是 PATH，查不到就误判成"未安装"。

**教训**：Windows 上判断某工具是否安装，`Get-Command` 查不到 **≠ 未安装**。必须同时检查：

```powershell
Test-Path "$env:LOCALAPPDATA\Microsoft\WindowsApps\<tool>.exe"   # MSIX 别名
Get-AppxPackage -Name '<PackageName>'                            # MSIX 包注册状态
```

**已修复**：`WindowsApps` 目录已加入用户级 PATH，现在 `pwsh -NoProfile -Command '$PSVersionTable.PSVersion'` 输出 `7.6.5`，`Get-Command pwsh` 正常解析。

**这仍然影响 AGENTS.md 的有效性**（但性质变了）：文档里的 `pwsh scripts/build-signed.ps1` 现在可以执行了，但它会**走 PowerShell 7 的解析器**——7.x 默认按 UTF-8 读取无 BOM 文件，所以 §4.2 的 BOM 问题在 pwsh 下**不会触发**，只在 `powershell.exe`（5.1）下触发。这反而使问题更隐蔽：同一个脚本，用 `pwsh` 跑正常、用 `powershell.exe` 跑 ParserError。

> 附带发现：`%LOCALAPPDATA%\Microsoft\PowerShell` 下有 `7.6.0\`、`7.6.5\` 更新哨兵。

### 4.2 build-signed.ps1 缺 BOM（我独立复核确认）

| 文件 | 前三字节 | PS 5.1 解析错误数 |
|------|---------|-----------------|
| `scripts/build-signed.ps1` | `3c230a`（**无 BOM**） | **3** |
| `scripts/build-single.ps1` | `efbbbf`（有 BOM） | 0 |
| `scripts/asar-scan.ps1` | `2320e5`（无 BOM） | **0** |

实测报错：

```
所在位置 scripts\build-signed.ps1:53 字符: 1
+ }
表达式或语句中包含意外的标记"}"。
所在位置 scripts\build-signed.ps1:84 字符: 11
+     throw @"
字符串缺少终止符: "@。
```

**根因不是"无 BOM"本身**——`asar-scan.ps1` 同样无 BOM 却解析正常。真正原因是非 ASCII 字节落在了会被解析器击穿的位置（`build-signed.ps1` 的中文 .DESCRIPTION 让第 53/84 行这些**不含中文的结构行**被连带击穿）。

报错信息本身也是乱码，AI 读到后很难立刻判断是编码问题，通常先怀疑引号、here-string、参数传递，反复试错。

**后果最严重的一点**：这个脚本的 .DESCRIPTION 里辛苦固化了两个坑的经验（缺口令时 tauri signer 阻塞 stdin、传路径报 `Invalid symbol 58`）——因为脚本跑不起来，这些经验完全无法发挥作用。

**修复**：

```powershell
$p = 'D:\workbuddy2api\scripts\build-signed.ps1'
$c = Get-Content -LiteralPath $p -Raw -Encoding UTF8
[System.IO.File]::WriteAllText($p, $c, (New-Object System.Text.UTF8Encoding $true))
```

**防回归**：在 AGENTS.md 加编码约定——「`.ps1` 若含中文必须存为 UTF-8 **with BOM**；`.sh` 必须 UTF-8 **no BOM**（POSIX 解析器不认 BOM，实测 7 个 .sh 均无 BOM，是正确状态）」，并在 ci.yml 加一步校验。

### 4.3 建议加进 AGENTS.md 的环境速查表

```markdown
## 本机环境（实测，勿再探测）

- Shell：本机**两种 PowerShell 都可用**，但行为不同！
  - `pwsh`（PowerShell 7.6.5，MSIX 版，位于 `%LOCALAPPDATA%\Microsoft\WindowsApps\pwsh.exe`）
    → 默认按 UTF-8 读取脚本，**能跑无 BOM 的中文 .ps1**
  - `powershell.exe`（Windows PowerShell 5.1）→ 默认码页 GBK，**跑无 BOM 的中文 .ps1 会 ParserError**
  - 结论：AGENTS.md 里的 `pwsh scripts/build-signed.ps1` 是对的；若要改用 `powershell.exe`，
    必须先确认目标 .ps1 带 UTF-8 BOM。
- Rust 工具链在 `C:\Users\T1367\.cargo\bin`，**已加入用户级 PATH**（2026-09-15）。
- 已装：go 1.25.5 / node v26.8.2 / npm / python 3.14.2 / gh / 完整 Git Bash / pwsh 7.6.5
- 未装：winget
- 磁盘：NVMe SSD，D 盘 NTFS 余量充足

## 文件编码约定（重要）

- `.ps1` 若含中文，**必须存为 UTF-8 with BOM**。
  否则在 `powershell.exe`（5.1，GBK 默认码页）下会 ParserError，且报错行号会落在
  **不含中文的结构行**上（如 param 块的收尾 `}`），极难排查。
  自己用 `cat > x.ps1` 或编辑器生成脚本时尤其注意——它们默认**不加** BOM。
- `.sh` 必须 UTF-8 **no BOM**（POSIX 解析器不认 BOM，加了会让 shebang 失效）。
```

---

## 五、CI 层

### 5.1 两个 workflow 触发条件逐字相同

```
build.yml:3-6          release.yml:27-30
on:                    on:
  push:                  push:
    tags: ["v*"]           tags: ["v*"]
  workflow_dispatch:     workflow_dispatch:
```

**且两者都没有 `concurrency`**（只有 ci.yml 和 pages.yml 有）。每次打 tag → 两条完整流水线并发 → 各自构建 NSIS 安装包 → 各自对同一 tag 发一次 Release。

> ⚠️ **不要草率删除 build.yml**：`npm publish`（平台包 + 主包）只有 build.yml 有，release.yml 完全不发布 npm。
>
> 另外经核实：build.yml 的矩阵**只有 win-x64 一项**（注释写着"macOS / Linux 支持已移除"），所以重复的只是 Windows 一条，不是三平台——重复量约为 release 的 1/3，不是"翻倍"。
>
> **重要背景**：`build.yml` 与 `pages.yml` 目前**未纳入 git 跟踪**（`git status` 显示 `??`），而工作区里未提交的 release.yml 改动恰好补上了 `continue-on-error`、`!cancelled()`、签名校验、updater 清单。也就是说线上 v0.7.2 实际生效的是 build.yml，而更完备的 release.yml 还没提交。

**建议**：把 build.yml 的 npm 发布步骤并到更完备的 release.yml，然后给 build.yml 只保留 npm 职责（删掉 `Build desktop app` / `Generate updater manifest` / upload-artifact），并删除它整个 `release:` job（与 release.yml 重复）；同时给两个 workflow 都加 `concurrency`。

### 5.2 缓存策略分裂

| Workflow | 缓存方式 |
|----------|---------|
| `ci.yml` | `Swatinem/rust-cache@v2` |
| `release.yml` | `Swatinem/rust-cache@v2` + `key: ${{ matrix.target }}` |
| `build.yml` | `actions/cache@v4`，path 含**整个 `target`**，无排除 |

`actions/cache` 无 exclude 语法，把 target 全量纳入会导致缓存体积远超仓库 10GB 上限。而 build.yml 自己的注释（第 138 行）就写着"cargo cache 会保留旧 NSIS 安装包；不清理就会把 0.1.17 一类产物带进 Release"——等于自证缓存内容不可信。

**修复**：换成 `Swatinem/rust-cache@v2`，与 release.yml 一致。

> 更正：审计初稿说"缓存命中率接近 0、等于冷编译"是**夸大**——本机实测仓库缓存总计仅 3.4GB，且 v0.7.1→v0.7.2 间隔 22 分钟的两次发布都成功，反证 restore 能命中。真实问题是不一致与体积浪费，不是"完全失效"。

### 5.3 其他 CI 问题

- `build.yml:96` 用 `npm install` 而非 `npm ci`（其他三条都用 npm ci）。`npm install` 会静默更新 lockfile 与传递依赖 → **CI 构建不可复现**。
- `build.yml` 未配 `setup-node` 的 `cache: npm`（其他三条都有）。
- `pages.yml` 每次 push 到 main 都跑一次完整前端构建并部署，与 ci.yml 的前端构建重复（一次 push 到 main，`tsc` 共跑 4 次）。
- `ci.yml` 每次 push 都在 3 个平台跑 `cargo check --workspace --all-targets`（`--all-targets` 会连测试代码和 examples 一起编）。

---

## 六、依赖层（可量化收敛）

### 6.1 Cargo.lock 现状

- **618 个包**，533 个唯一 crate 名
- **50 个 crate 存在多版本共存**，重复编译单元总数 **85**

分裂最严重的：

| crate | 版本数 | 版本 |
|-------|-------|------|
| `windows-sys` | 6 | 0.45.0, 0.48.0, 0.52.0, 0.59.0, 0.60.2, 0.61.2 |
| `windows-targets` | 4 | — |
| `syn` | 3 | 1.0.109, 2.0.119, 3.0.3 |
| `hashbrown` / `getrandom` / `rand_core` / `dirs` / `windows-core` | 3 each | — |
| `thiserror` / `base64` / `bitflags` / `indexmap` | 2 each | — |

> 重要更正：`windows-sys 0.45.0` / `0.52.0` 看似"幽灵"，实则被**非宿主平台**的 cfg 门控依赖引用（jni→Android、ring→aarch64 Windows）。Cargo.lock 对所有 `[target.'cfg(...)'.dependencies]` 一律无条件列出，这是**正常行为**，不是可以手删的垃圾。手改 Cargo.lock 会破坏图一致性。

### 6.2 两项高性价比修复（已逐处验证）

**① `dirs = "5"` → `"6"`**（`ai-gateway-core/Cargo.toml:13` 与 `ai-gateway-router/Cargo.toml:20`）

- `cargo tree -i dirs@5.0.1` 的引入方**只有本项目这两个 crate**
- `dirs@6.0.0` 的引入方全是第三方（tauri、tauri-build），已在树中
- 用到的 4 个函数（`home_dir` / `data_local_dir` / `data_dir` / `config_dir`）在 dirs 5 与 6 中签名完全相同
- **可消掉 `dirs 5.0.1` + `dirs-sys 0.4.1` + `windows-sys 0.48.0` 三个编译单元**
- 非 Windows 平台上收益更大（可三合一 dirs 4/5/6）

**② `windows = "0.58"` → 改用 `windows-sys 0.61.2`**（`ai-gateway-core/Cargo.toml` 的 `[target.'cfg(windows)'.dependencies]`）

- `libwindows.rlib` 单个 **99.7 MB**（全项目最大依赖），且编了 **4 份**（重复浪费 131.8 MB）
- 项目只用到 9 个 Win32 符号，**逐个到 windows-sys 0.61.2 源码核对全部存在**：
  - `CreateJobObjectW` / `AssignProcessToJobObject` / `SetInformationJobObject` / `IsProcessInJob` → `System/JobObjects/mod.rs`
  - `CloseHandle` / `LocalFree` → `Foundation/mod.rs`
  - `CryptUnprotectData` → `Security/Cryptography/mod.rs:315`
  - `PCWSTR` / `HANDLE` → `core/mod.rs` / `Foundation/mod.rs`
- **可消掉 `windows 0.58.0` + `windows-core 0.58.0` + `windows-implement 0.58.0` + `windows-interface 0.58.0`**

### 6.3 两项"看起来能修但实际不能"（已证伪）

- **`ring`（C/汇编编译）不是问题**：实测 out 目录只有 16 个 `.o` 加两个 770,906 字节静态库，共 **1.1 MB**，且被 rustls 单一链条引入无重复编译。换 native-tls 属优化错位。
- **`rusqlite` bundled 不能去掉**：`sqlite3.o` 实测 4.6 MB 确实是最重的 C 编译项，但代码**真实需要可写连接**——`session.rs:271` 的 `insert_session_copy` 与 `session.rs:331` 的 `insert_edge_sync_mapping` 传 `read_only=false` 执行 `INSERT OR REPLACE`，`vscode_cn_inject.rs:493-527` 还需 `CREATE TABLE`。

---

## 七、target 目录卫生

实测总体积 **42 GB**（两轮审计间因其他构建活动从 31G 涨到 42G）：

| 目录 | 体积 | 说明 |
|------|------|------|
| `target/debug/deps` | 19 GB | 含 12 个 `.tmp*.temp-archive` **孤儿残留 2.19 GB**（rustc 中断留下，cargo 永不回收） |
| `target/debug/incremental` | 15 GB | 161 个目录 |
| `target/debug/build` | 1.5 GB | 含 21 份 `gateway_embed.rs`（310 MB，去重后只 6 种） |
| `target/release` | 3.3 GB | |

**incremental 对 `ai-gateway-core` 完全无效**：161 个目录中 `ai_gateway_core` 占 34 个 / 12 GB（平均 354 MB/目录）。因为 `build.rs` 每次重跑都改写 15.5MB 的 `OUT_DIR/gateway_embed.rs`，而它被 `include!` 进 lib，命中它的查询全部失效——**缓存写了，下一轮一个字节都复用不上**。

**所以 `CARGO_INCREMENTAL=0` 对本项目是正确选择**：唯一的大头（core）本来就用不上。

**多份 `ai-gateway-<hash>` 的成因**（读 `.fingerprint/*.json` 逐份比对得出）：不是 `--target`，而是 **profile + feature 双变量漂移**。同 target、同 path 下出现三种 lib 组合（`default+custom-protocol × profileA`、`× profileB`、`无 feature × profileB`）。审计中途还实抓到运行中的命令是 `cargo build --bins --features tauri/custom-protocol --release`（手工会话），与 `npm run tauri dev`、`build-signed.ps1` 交替。

**结论**：`CARGO_TARGET_DIR` **不建议收敛**——dev/release 混用同一 target 是 cargo 默认且正确的行为，真正的问题是指纹漂移而非目录分散。设了只会把 42 GB 搬到别处。

### 建议的清理脚本（PowerShell，符合 AGENTS.md 的 Shell Policy）

```powershell
# 先断言没有 cargo/rustc 在跑（有则退出）
if (Get-Process cargo,rustc -ErrorAction SilentlyContinue) {
    throw "有 cargo/rustc 进程在运行，先停止再清理"
}
$root = 'D:\workbuddy2api\target\debug'

# 可安全删除
Remove-Item -Recurse -Force "$root\incremental"            -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force "$root\examples"               -ErrorAction SilentlyContinue
Remove-Item -Force "$root\deps\.tmp*.temp-archive"         -ErrorAction SilentlyContinue
Get-ChildItem "$root\build" -Directory |
    Where-Object { $_.Name -like 'ai-gateway-core-*' } |
    ForEach-Object { $_.FullName } | Write-Host   # 先列出，确认后删

# 必须保留：target\debug\deps\*.rlib|*.rmeta、target\debug\.fingerprint、target\release\*
```

> `cargo clean -p ai-gateway-core` **不是好工具**——它会连带删掉 debug/release 两侧该 crate 的 rlib 与指纹，导致下游三套 rlib 重链，代价很大。直接删 `target/debug/build/ai-gateway-core-*` 只删 build 目录、保留 rlib。

---

## 八、前端层

| 项目 | 实测值 |
|------|--------|
| `tsc --noEmit` | 3.65–4.29 秒（**未开 incremental/composite**，且 `tsc` 裸调用让 tsconfig 的 `references` 完全不生效） |
| `vite build` | 9.87 秒 / 2584 modules |
| 产物 | 单一主 chunk `index-Dco1sn0r.js` = **1,102,746 字节**（gzip 323.39 kB），触发 Vite 500kB 警告 |
| `node_modules` | 156.36 MB |

**问题**：

1. **单一 1.1MB chunk，无代码分割**：recharts 在主 chunk 里出现 110 次，全部路由被打进主包
2. `vite.config.ts` 的 `server.watch.ignored` **覆盖了 Vite 默认忽略列表** → `dist/` 失去保护
3. `package-lock.json` 根 `version` 是 **0.5.0**，落后 package.json（0.7.2）**9 个版本**，且无任何 CI 校验拦截
4. `tsc` 未开 `incremental`

**node_modules top 5**：`lucide-react` 29.78 MB / `typescript` 21.81 MB / `@tauri-apps` 15.62 MB / `.vite` 13.33 MB / `@esbuild` 11.15 MB

> 更正：`lucide-react` 用的是全量具名导入，**已经能被 tree-shake**，不是问题——29.78 MB 是安装体积（含全部图标源文件），不是打包体积。

---

## 九、Rust profile 与 release 构建

仓库**零 profile 配置**（根 `Cargo.toml` 全文 8 行只有 `[workspace]`，`.cargo/config.toml` 不存在），dev/release 全走 Cargo 默认值。

实测 release 构建的异常：`libai_gateway_lib.lib` 等 debug 产物堆积，且 **release 主程序 23.4 MB 仍附带 8.1 MB pdb**。

**建议追加到根 `Cargo.toml`**（必须写在 workspace 根，写进 src-tauri 会被忽略）：

```toml
[profile.dev]
# 只保留文件+行号，丢掉变量/类型信息。
# 实测本机 debug PDB 已达 375MB（单个 201MB）。
debug = "line-tables-only"
incremental = true

[profile.release]
# thin-LTO：584 个 rlib 之间可跨 crate 内联
lto = "thin"
codegen-units = 16
# 剥掉主程序 exe 的调试符号。签名对象是 NSIS 产出的 setup.exe，与此无关。
strip = true
```

> **验证 agent 的重要更正**：
> - 收益最大项不是"回收 PDB"，而是 **`.lib`/`.rlib` 占了 13.06 GB / 1041 个文件**（单文件最大 1.17 GB）
> - `strip=true` **不影响 updater 签名**——实测 `target/release/bundle/nsis/*.sig` 440 B 正常生成
> - `[profile.dev.package."*"] opt-level = 2` 的收益对象有限：实际分发的网关是 **Go 二进制**（`embedded/gateway.exe`），完全不经过 cargo profile
> - **不要加 `panic="abort"`**
> - 首次改 profile 会触发一次全量重编

---

## 十、经证伪的怀疑（避免重复踩坑）

| 怀疑 | 实测结论 |
|------|---------|
| `pwsh` 可被 `powershell` 替代 | 不能——`powershell.exe` 存在，但执行 `.ps1` 时撞上 BOM 编码崩溃 |
| Windows Defender 拖慢构建 | **不成立**——WinDefend 服务 Stopped，`Get-MpPreference` 报错，无第三方 AV |
| D 盘是慢盘 / 空间不足 | **不成立**——NVMe SSD（致钛 TiPlus7100 + YMTC PC411），D 盘余量充足 |
| Windows 长路径导致构建失败 | **不成立**——`LongPathsEnabled=1`，最长路径 163 字符，>260 的为 0 |
| git hooks 拖慢操作 | **不成立**——`.git/hooks/` 下无任何非 `.sample` 文件 |
| 需要设 `CARGO_TARGET_DIR` 收敛 | **不建议**——dev/release 混用同一 target 是正确行为，收益为负 |
| `ring` 的 C 编译是重项 | **不成立**——仅 1.1 MB 产物，无重复编译 |
| `rusqlite` bundled 可去掉 | **不能**——代码真实需要可写连接（INSERT/CREATE TABLE） |
| `windows-sys 0.45/0.52` 是可手删的幽灵 | **不能删**——被非宿主平台的 cfg 门控依赖引用，手删会破坏图一致性 |
| `lucide-react` 未 tree-shake | **不成立**——全量具名导入已可 tree-shake |
| `build.yml` 可直接删除 | **不能**——`npm publish` 只有它有 |
| `Compression::best()` 的 10% 体积被磁盘块对齐吞掉 | **不成立**——实测 level 9 与 level 6 对齐后为 3,371,008 vs 3,457,024，差异可见 |

---

## 十一、行动清单

### 立即做（今天，约 5 分钟）

- [x] ~~把 `%USERPROFILE%\.cargo\bin` 加进用户级 PATH~~ ✅ **已完成**（同时加了 `WindowsApps`，让 pwsh 可用）
- [ ] **重启终端和 IDE**（已运行的进程不继承新 PATH —— 这是 PATH 修改生效的必要条件）
- [ ] 给 `scripts/build-signed.ps1` 加 UTF-8 BOM
- [ ] 把环境速查表与文件编码约定加进 AGENTS.md 顶部（见 §4.3）

### 本周做（约 1 小时）

- [ ] 改 `crates/ai-gateway-core/build.rs`：`include_bytes!` + `Compression::new(6)` + 条件化 `rerun-if-changed`，同步改消费侧 4 行
- [ ] `dirs = "5"` → `"6"`（两个 Cargo.toml）
- [ ] 追加 `[profile.dev] debug = "line-tables-only"` 到根 Cargo.toml
- [ ] 清理 `target/debug/incremental`、`target/debug/examples`、`.tmp*.temp-archive` 孤儿

### 计划做（需要验证 CI）

- [ ] 合并 `build.yml` 与 `release.yml` 的发布职责，加 `concurrency`
- [ ] `build.yml` 的 `actions/cache` → `Swatinem/rust-cache@v2`
- [ ] `build.yml` 的 `npm install` → `npm ci` + `cache: npm`
- [ ] 修 `package-lock.json` 的根 `version`（0.5.0 → 0.8.0）并加 CI 校验
- [ ] 把 `windows = "0.58"` 换成 `windows-sys 0.61.2`（需逐个符号迁移验证）

---

## 附录：审计方法与可信度

| 轮次 | 维度 | agent 数 | 成功 | 失败 |
|------|------|---------|------|------|
| 第一轮 | 8 维度深挖 + 验证 + 补漏 | 45 | 41 | 4（API 503） |
| 第二轮 | 补跑丢失维度 + build.rs 修复设计 | 40 | 40 | 0 |

**每条发现都经过独立的"反驳式"验证 agent**——它必须自己去读文件、跑命令，不许引用原发现的措辞。验证 agent 确实多次纠正了初稿：行号偏差、夸大数字（"runner 分钟数"无依据）、因果倒置（把历史注释当成当前配置的必然结果）。

**本报告中的数字全部为本机实测**，包括我自己独立复现的关键项：
- `cargo check -p ai-gateway-core` 连续两次 6.19s / 6.38s，每次都 `Compiling`
- 字面量版编译 8,791ms vs `include_bytes!` 版 511ms
- 三个 `.ps1` 的 BOM 字节与解析错误数
- gzip 四档压缩的耗时与体积
- target 42G、node_modules 156MB

**已知的可信度缺口**：审计 agent 曾用 `gh api` 查询线上缓存状态（返回 3.4GB），但 Release 的 draft 状态与实际 runner 分钟数无法在只读离线环境核实——报告中涉及这些的部分已标注为推断或已删除。

**审计本身出错并已更正的一条**：初稿断言"本机未安装 pwsh"，**这是错的**——pwsh 7.6.5 以 MSIX 形式装着，只是 `WindowsApps` 别名目录不在 PATH 中、且我探测时只查了 MSI 版的安装路径。这是**我的判断失误**，不是验证 agent 的问题（它沿用了我的错误前提）。更正内容见 §4.1。**教训：`Get-Command` 查不到 ≠ 未安装，Windows 上必须同时查 `%LOCALAPPDATA%\Microsoft\WindowsApps\` 与 `Get-AppxPackage`。**
