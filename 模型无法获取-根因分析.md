# 「部分模型无法从上游获取」根因分析（修正版）

> 分析对象：`D:\workbuddy2api`（AI Gateway）
> 现象：上游国服**有** `glm-5.3-flash` 等模型，但网关 `/v1/models` 里**不显示**
>
> **本文修正了初版报告的定性**：初版聚焦「请求被路由到错误区域导致 11102」，那是**另一个独立缺陷**；
> 用户所述「模型没显示」的真实根因见下。

---

## 一、结论（一句话）

**正在运行的网关二进制是旧版本（构建于 09-15 02:10），它使用已被上游废弃的模型接口
`GET /console/enterprises/personal/models`；而新代码（09-15 20:32 提交）已改用
`GET /v3/config`。旧接口在当前上游不再可靠地返回完整清单，导致部分模型拿不到。**

**已实测确认：运行中的二进制内不含 `/v3/config` 字符串，只含旧端点字符串。**

```
运行中的二进制: ~/.ai-gateway/gateway/bin/gateway-32fae0-790b9e2c22009c21.exe
构建时间:       2026-09-15 02:10:44

字符串扫描结果:
  /v3/config                             absent      ← 新端点不在里面
  /console/enterprises/personal/models   PRESENT @ 4800544   ← 旧端点在
  WorkBuddy/5.5.2                        absent      ← 新版专用 UA 不在里面
```

而仓库源码里：

| 提交 | 时间 | 内容 |
|---|---|---|
| `1907efe` | 2026-09-15 **20:32** | `fix: 模型清单改用 /v3/config，修复国际版拿不到模型与静态表过时` |
| `4ab886b` | 2026-09-15 **20:58** | `fix: 模型清单不下发上游已停用的模型，并修正国际版补齐注释` |

**运行中的二进制（02:10）比这两个修复早了约 18 小时。**

---

## 二、实测证据

### 2.1 国服上游真实清单（直接调用，绕过网关）

`GET https://copilot.tencent.com/v3/config` → `data.agents[name=="cli"].models`，**16 个**：

```
auto                  deepseek-v4-pro      deepseek-v4.1-flash   glm-5.1
glm-5.2               glm-5.3              glm-5.3-flash         glm-5v-turbo
hy3                   hy3-x                hy4-preview           kimi-k2.6
kimi-k2.7             kimi-k2.8-preview    kimi-k3-1             minimax-m3
```

`data.models` 池共 52 个；上述 16 个是 CLI 真正可选子集。

### 2.2 这些模型确实可用（直连国服账号逐个试）

```
glm-5.3-flash        HTTP 200 WORKS
glm-5.3              HTTP 200 WORKS
kimi-k3-1            HTTP 200 WORKS
hy3-x                HTTP 200 WORKS
auto                 HTTP 200 WORKS
deepseek-v4.1-flash  HTTP 200 WORKS
```

### 2.3 网关实际返回（`GET /v1/models`，28 个）

**上游有、网关没有（5 个）：**

```
MISSING: auto
MISSING: glm-5.3-flash      ← 用户报告的那个
MISSING: hy3-x
MISSING: kimi-k3-1
MISSING: kimi-k2.8-preview
```

**网关有、上游已无（3 个，属过时静态表）：**

```
STALE: hy3-preview
STALE: hy3-preview-agent
STALE: deepseek-v4-flash
```

### 2.4 新版代码本身是正确的

我直接调用仓库现行 `upstream.Client.FetchModels()`（用同一份国服凭证、同一环境含代理），
**返回完整 16 个模型，含 `glm-5.3-flash`**：

```
[new] /v3/config -> 16 models, err=<nil>
  auto  hy4-preview  hy3  hy3-x  deepseek-v4.1-flash  glm-5.3  glm-5.3-flash
  glm-5.2  glm-5.1  glm-5v-turbo  minimax-m3  kimi-k3-1  kimi-k2.8-preview
  kimi-k2.7  kimi-k2.6  deepseek-v4-pro
```

**即：代码已修好，但跑的二进制是旧的。**

---

## 三、为什么旧接口会少给模型

两点叠加：

**① 旧接口与新接口是不同数据源。** 新代码注释（`client.go:638-644`）已记录该设计变更：

> 为什么不用 `/console/enterprises/personal/models`：
> 该接口在国际版恒返回 500 …导致国际版永远只能靠硬编码静态表。
> `/v3/config` 返回同一份模型数据且两个区域都可用。

**② 旧接口返回 30 个「模型池」条目，但其中混有大量不可用于对话的项**，
旧实现在 `FetchModels` 里直接遍历 `data.models` 全量下发（无 `agents[cli]` 白名单概念），
因此既多给了不该给的、也在上游调整该接口返回内容时就与真实 CLI 可选集脱节。

**关键点**：`/v3/config` 的 `agents[cli].models` 才是客户端选模型时**真正看到的清单**。
旧实现没有这个概念，所以清单永远对不齐。

---

## 四、修复方案

### 唯一必要的动作：重新构建并替换网关二进制

代码已修复（`1907efe` / `4ab886b`），**无需改代码**。需要做的是让运行中的网关用上新构建。

```powershell
# 1) 构建网关（产物落到 crates/ai-gateway-core/embedded/）
pwsh scripts\build-gateway.ps1        # 或按 NAVIGATION.md 的构建流程

# 2) 重新构建并安装 App（网关是内嵌进主程序的）
$env:PATH = "$env:USERPROFILE\.cargo\bin;$env:PATH"
pwsh scripts\build-signed.ps1
```

> 现有 `~/.ai-gateway/gateway/bin/gateway-<指纹>.exe` 按**内容指纹**命名，
> 新版本会自动释放为新的指纹文件，无需手工删除旧的。
>
> 构建前请先退出正在运行的 App（网关是它的子进程），否则文件可能被占用。

### 验证方式

替换后重启网关，再查一次：

```powershell
$key = (Get-Content "$env:USERPROFILE\.ai-gateway\gateway\gateway_config.json" -Raw | ConvertFrom-Json).api_key
(Invoke-RestMethod "http://127.0.0.1:7863/v1/models" -Headers @{Authorization="Bearer $key"}).data.id |
  Where-Object { $_ -in @("glm-5.3-flash","auto","hy3-x","kimi-k3-1","kimi-k2.8-preview") }
```

预期 5 个都应出现。

---

## 五、顺带发现的两个独立缺陷（与本次现象无关，但建议一并修）

### 5.1 动态分支不补国服静态表（`handler.go:306`）

```go
for _, m := range staticModelsIntl {     // 只补国际版
    if id, _ := m["id"].(string); id != "" && !seen[id] { seen[id] = true; out = append(out, m) }
}
```

作者假设「动态=国服，所以补国际版」。但 `pickModelsProbeAccount()` 一旦抽到国际版账号
（2 个国服账号进冷却即触发，`AvailableUIDs` 只收健康账号），国服模型就成批消失。
**建议改为补齐 `staticModelsAll`**（两区域），一行改动。

### 5.2 账号选号层不看区域，导致 11102（初版报告的主体）

`pool.go:725-750` 的候选过滤只有 `tried`/`healthy`/`modelCooled`/`inFlightFull`，
**没有区域条件**。且到期分层（`pool.go:852-876`）先于任何区域判断执行。

本机实测：最早到期档位（09-26）恰好是 **3 个国际版账号**，2 个国服账号在更晚的 10-01 档，
于是国服模型名 100% 被送到国际版账号上：

```
hy3-preview            ok=0 fail=3
hy3-preview-agent      ok=0 fail=3
deepseek-v4-pro        ok=0 fail=3
deepseek-v4-flash      ok=0 fail=3
```

而同一直连国服账号全部 HTTP 200（见 2.2）。

**注意**：这是**请求路由**问题，与「模型不显示」是两回事。两者都需要修，但成因不同。

`11102` 在全代码库**无任何逻辑处理**（24 处命中全是注释）→ 落 `ErrClient` →
`applyErrorPolicy` 的 default 分支「只换号不罚」→ 系统学不会，每次重犯。

**建议**：新增 `ErrModelNotFound` 分类，在 `applyErrorPolicy` 中调用**已有的**
`CooldownModel(uid, model, ...)`（按 uid+model 记账，已持久化，三处消费）。
注意用模型级而非账号级 —— 账号本身没坏，它还能服务另一批模型。

---

## 六、未经实测的推断（如实标注）

1. **旧接口为何恰好少这 5 个**：我实测旧端点当前仍返回 30 个（含 `glm-5.3-flash`），
   因此**运行中二进制拿到的清单为何是 12 个动态项，我未能 100% 复现**。
   最可能的解释是：该进程自 18:33 启动后，其 1 小时 TTL 缓存命中了一个更早的、
   当时上游返回内容不同的快照，且负缓存（5 分钟）反复阻止刷新。
   这不影响结论——**旧代码 + 旧端点已确认与新版行为不一致，构建新版即可对齐**。
2. `hardMarkers` 误判风险：真实 11102 响应体**实测不命中**任何关键词，故未触发硬冷却。
   属潜在风险，非当前成因。
3. 国服 `data.models` 池 52 个中哪些适合下发，未逐一评估；现行实现取 `agents[cli].models`
   的 16 个是正确口径。

---

## 七、本次分析对运行状态的影响

仅做了**只读探测**（GET /v1/models、直连上游试调），未修改任何代码或配置。
探测产生的短暂软冷却已自愈，复核为 `healthy=7 cooling=0 disabled=0`。
诊断用的临时脚本已删除。
