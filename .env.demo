# 演示 / 截图模式的 Vite 环境变量。
#
# 为什么用 mode 文件而不是 npm script 里的内联变量：`VAR=1 vite build` 是 POSIX 写法，
# 在 Windows 的 cmd / PowerShell 下直接失败（提示「不是内部或外部命令」）。
# 仓库运行环境是 Windows（见 AGENTS.md Shell Policy），因此改用
# `vite --mode demo`，由本文件提供变量。
VITE_DEMO_MODE=1
