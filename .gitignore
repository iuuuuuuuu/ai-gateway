# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri 构建产物（workspace target）
# target-* 覆盖 CARGO_TARGET_DIR 指向的自定义目录（如 target-check/），
# 单纯的 target/ 不匹配它们，会漏进 git status。
target/
target-*/
src-tauri/target/

# 本地 cargo 配置（sccache 等本机专属设置）。
# 必须排除：rustc-wrapper 指向不存在的程序时 cargo 会**直接报错**而非降级，
# 而 CI 三平台都没装 sccache —— 提交进去会让所有 job 构建失败。
.cargo/config.toml

# npm 包 postinstall 下载的平台二进制
npm/bin/ai-gateway-*

# 本地运行时数据
accounts.json
backups/

# Editor directories and files
.vscode/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
npm/platform/*/bin/

# AI 工具本地配置
.codebuddy/
.cursor/
.agents/
.codex/
.grok/
/.workbuddy/

# 内嵌网关二进制（构建产物，由 scripts/build-gateway 生成）
crates/ai-gateway-core/embedded/

# 单文件构建产物（不入库，改由 CI/本地构建生成）
dist-single/
_rel/

# CI 工作流已纳入版本管理（.github/workflows/）。
# 注意：推送工作流文件需要 GitHub token 具备 workflow scope。

# 更新签名私钥（绝不入库；丢失后无法签名新版本）
*.key
*.key.pub
!.gitignore

# AI 助手的本机仓库导航备忘（只在本地用，不入库）
NAVIGATION.md

# 本机 AI 生成的内部审计/分析报告（含本机绝对路径，只在本地参考，不入库）。
# 用显式文件名而不是通配：仓库里另有正经文档（如本 README），
# 通配 *.md 会误伤。
/构建瓶颈审计报告.md
/模型无法获取-根因分析.md

# Python 缓存（仓库根出现过 __pycache__/*.pyc 被误加进暂存区）
__pycache__/
*.pyc

# 本地隔离测试实例的 Tauri 配置（独立 identifier + 数据目录，仅供开发调试）
src-tauri/tauri.dev-sandbox.conf.json

# 本地构建出的待发布产物（安装包 + .sig + updater 清单）。
# 这些只应上传到 GitHub Release，不入库：安装包 11 MB 量级，进 git 历史后
# 永远删不掉，且公开仓库没必要存二进制。发布走 release.yml 或 gh release upload。
/dist-bin/
# 打包的**统一输出目录**（scripts/package-release.ps1 固定写到这里）。
# 与 dist-bin/ 同理不入库：只是待上传的产物，不是源码。
/release-out/

# 本机/私有的 env 文件（可能含密钥）。仓库里有两份**故意入库**的演示构建配置
# `.env.demo` 与 `.env.pages-demo`，它们只含 VITE_* 构建开关、无任何密钥，
# 因此用 `!.env.*` 例外放行；其余 .env* 一律不入库。
.env
.env.*
!.env.demo
!.env.pages-demo

# Claude Code 的本机状态目录。
#
# `worktrees/` 是它自动建的 git worktree（每个约 300-400 MB，实测曾累计 392 MB
# 全是构建产物），`settings.local.json` 是本机权限配置 —— 都不该入库。
# `launch.json` 例外放行：它只是「用 pwsh 跑 scripts/tmp-preview-gateway.ps1」
# 这一条调试配置，无本机路径、对其他人同样可用，入库反而有用。
.claude/*
!.claude/launch.json
